export DOT_ZSH="${HOME}/.config/zsh"

# Initialize
source ${DOT_ZSH}/load-plugins.zsh
source ${DOT_ZSH}/appearance.zsh
source ${DOT_ZSH}/general.zsh

# Environment variables

# Aliases
alias ssh-config="zed ~/.ssh"
alias op=opencode
alias z=zed
alias ip="ip -c"
alias ll="ls -l"
alias la="ls -la"
